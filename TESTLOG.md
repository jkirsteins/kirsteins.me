# Test Log

Invariants and success criteria that must never be violated.
Each entry represents a property of the system that has been verified and must remain true.

## Invariants

- Site builds successfully with `npm run build`
- All expected pages are generated: home, articles index, projects, individual articles
- CSS is copied to output as a passthrough
