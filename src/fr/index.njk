---
layout: layouts/base.njk
---
<h2>{{ "stuff_title" | i18n }}</h2>

<ul>
  <li><a href="https://vardulis.kirsteins.me" target="_blank" rel="noopener">Wordlike</a> — {{ "vardulis_description" | i18n }}<a href="https://apps.apple.com/us/app/wordlike/id1619500618" target="_blank" rel="noopener">App Store</a> et sur le <a href="https://vardulis.kirsteins.me" target="_blank" rel="noopener">web</a>.</li>
</ul>

{% set articles = collections.articles_all %}
{% if articles.length %}
<h2>{{ "articles_title" | i18n }}</h2>

<ul>
  {% for article in articles | reverse %}
  <li>
    <a href="{{ article.url }}">{{ article.data.title }}</a>
    <span class="muted">{{ article.date | dateDisplay(lang) }}</span>
    {% set langs = article | translations %}
    {% if langs.length > 1 %}
      <span class="muted">({% for t in langs %}<a href="{{ t.url }}">{{ t.lang }}</a>{% if not loop.last %} · {% endif %}{% endfor %})</span>
    {% endif %}
  </li>
  {% endfor %}
</ul>
{% endif %}
